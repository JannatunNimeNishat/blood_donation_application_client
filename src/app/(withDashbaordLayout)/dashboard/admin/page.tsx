import { getUserInfo } from '@/services/actions/auth.services';


const AdminDashboard = () => {
  
    return (
        <div>
            <h1 className='text-3xl font-semibold text-center'>Welcome back, Admin</h1>
        </div>
    );
};

export default AdminDashboard;